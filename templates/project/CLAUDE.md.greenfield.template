<!--
═══════════════════════════════════════════════════════════════════════════════
WORKFLOW BOOTSTRAP - CLAUDE MUST READ THIS FIRST
═══════════════════════════════════════════════════════════════════════════════

Project Type: GREENFIELD (new project)
Initialized: {{DATE}}

INSTRUCTIONS FOR CLAUDE:

1. READ the orchestrator: {{WORKFLOW_HOME}}/agents/workflow-orchestrator.md

2. CHECK "Workflow State" section below

3. FOLLOW orchestration protocol (auto-chain agents)

4. FOLLOW agent utilization rules below (quality gates, debugging)

5. UPDATE state after each significant action

═══════════════════════════════════════════════════════════════════════════════
-->

# {{PROJECT_NAME}}

## 🔄 WORKFLOW ACTIVE

This project uses **Claude Workflow Agents**.

**Claude: Read the bootstrap comment above, then follow the Agent Utilization Guide below.**

---

## Agent Utilization Guide

**Claude: These rules are MANDATORY. Follow them automatically.**

### Quality Gates (Automatic After Code Changes)

```
AFTER ANY CODE CHANGE:
│
├─► code-reviewer
│   • Review new/modified files
│   • Check: quality, security, intent compliance
│   • If issues: show summary, suggest fixes
│   • If clean: continue silently
│
├─► Run tests (if exist)
│   • npm test / pytest / cargo test
│   • If fail: STOP, show failures, ask to fix
│   • If pass: continue
│
└─► Update CLAUDE.md state
```

### L2 Building Flow (Per Feature)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ FEATURE: [name]                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  backend-engineer                                                           │
│       │                                                                     │
│       ▼                                                                     │
│  ┌─────────────────┐     ┌─────────────────┐                               │
│  │  code-reviewer  │ ──► │   Run tests     │                               │
│  │  (AUTO)         │     │   (if exist)    │                               │
│  └─────────────────┘     └────────┬────────┘                               │
│                                   │                                         │
│                          PASS?────┼────FAIL?                                │
│                            │      │      │                                  │
│                            ▼      │      ▼                                  │
│                       Continue    │   debugger                              │
│                            │      │   (fix, retry)                          │
│       ┌────────────────────┘      │                                         │
│       ▼                           │                                         │
│  frontend-engineer                │                                         │
│       │                           │                                         │
│       ▼                           │                                         │
│  ┌─────────────────┐     ┌────────┴────────┐     ┌─────────────────┐       │
│  │  code-reviewer  │ ──► │   Run tests     │ ──► │  ui-debugger    │       │
│  │  (AUTO)         │     │   (if exist)    │     │  (if puppeteer) │       │
│  └─────────────────┘     └────────┬────────┘     └────────┬────────┘       │
│                                   │                       │                 │
│                          PASS?────┼────FAIL?              │                 │
│                            │      │      │                │                 │
│                            ▼      │      ▼                │                 │
│                       Continue    │   debugger/           │                 │
│                            │      │   ui-debugger         │                 │
│       ┌────────────────────┘      │                       │                 │
│       ▼                           │                       │                 │
│  test-engineer                    │                       │                 │
│       │                           │                       │                 │
│       ▼                           │                       │                 │
│  ┌─────────────────┐              │                       │                 │
│  │  Run ALL tests  │              │                       │                 │
│  └────────┬────────┘              │                       │                 │
│           │                       │                       │                 │
│  PASS?────┼────FAIL?              │                       │                 │
│    │      │      │                │                       │                 │
│    ▼      │      ▼                │                       │                 │
│ ┌──────┐  │   ┌──────────┐        │                       │                 │
│ │DONE  │  │   │Fix tests │        │                       │                 │
│ └──┬───┘  │   └──────────┘        │                       │                 │
│    │      │                       │                       │                 │
├────┼──────┴───────────────────────┴───────────────────────┘                 │
│    ▼                                                                        │
│  FEATURE COMPLETE                                                           │
│    │                                                                        │
│    ├──► project-ops sync (update docs)                                      │
│    ├──► Update intent (mark promises KEPT)                                  │
│    ├──► Update CLAUDE.md state                                              │
│    ├──► CI verification (if configured)                                     │
│    └──► Continue to next feature                                            │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Issue Response Protocol

```
USER REPORTS ISSUE
       │
       ├─── "UI doesn't work" / "looks wrong" / "can't click"
       │           │
       │           ▼
       │    ┌─────────────────────────────────────────────────────────────┐
       │    │ ui-debugger                                                 │
       │    │  1. Navigate to problem area                                │
       │    │  2. Take screenshot                                         │
       │    │  3. Capture console errors                                  │
       │    │  4. Check network requests                                  │
       │    │  5. Inspect DOM                                             │
       │    │  6. Diagnose root cause                                     │
       │    │  7. Suggest/apply fix                                       │
       │    │  8. Verify fix                                              │
       │    └─────────────────────────────────────────────────────────────┘
       │
       ├─── "Error" / "crash" / "exception" / "doesn't work"
       │           │
       │           ▼
       │    ┌─────────────────────────────────────────────────────────────┐
       │    │ debugger                                                    │
       │    │  1. Understand the error                                    │
       │    │  2. Reproduce the issue                                     │
       │    │  3. Add logging if needed                                   │
       │    │  4. Trace to root cause                                     │
       │    │  5. Fix the issue                                           │
       │    │  6. Add test to prevent regression                          │
       │    │  7. Verify fix                                              │
       │    └─────────────────────────────────────────────────────────────┘
       │
       ├─── "Test failing" / "tests broken"
       │           │
       │           ▼
       │    ┌─────────────────────────────────────────────────────────────┐
       │    │ debugger + test-engineer                                    │
       │    │  1. Run failing tests                                       │
       │    │  2. Analyze failure output                                  │
       │    │  3. Determine: test wrong OR code wrong?                    │
       │    │  4. Fix appropriate side                                    │
       │    │  5. Run tests again                                         │
       │    │  6. Verify all pass                                         │
       │    └─────────────────────────────────────────────────────────────┘
       │
       └─── After ANY fix:
                    │
                    ▼
             ┌─────────────────────────────────────────────────────────────┐
             │ code-reviewer on changed files                              │
             │ Run relevant tests                                          │
             │ Update state if needed                                      │
             └─────────────────────────────────────────────────────────────┘
```

### CI/CD Verification

```
BEFORE FEATURE COMPLETE:

  ┌─────────────────────────────────────────────────────────────────────────┐
  │ Local CI Simulation                                                     │
  │                                                                         │
  │  IF package.json exists:                                                │
  │    npm run lint (if script exists)                                      │
  │    npm run typecheck (if script exists)                                 │
  │    npm run test                                                         │
  │    npm run build                                                        │
  │                                                                         │
  │  IF pyproject.toml/requirements.txt:                                    │
  │    ruff check . (if configured)                                         │
  │    mypy . (if configured)                                               │
  │    pytest                                                               │
  │                                                                         │
  │  IF scripts/verify.sh exists:                                           │
  │    ./scripts/verify.sh                                                  │
  │                                                                         │
  │  ALL MUST PASS before marking complete                                  │
  └─────────────────────────────────────────────────────────────────────────┘

AFTER FEATURE COMPLETE (suggest to user):

  "Feature complete and verified. Ready to commit?

   /project commit - Create conventional commit
   /project push   - Push to remote
   /project pr     - Create PR (requires GitHub MCP)"
```

### Agent Quick Reference

| Agent | Auto/Manual | Trigger |
|-------|-------------|---------|
| code-reviewer | AUTO | After ANY code changes |
| ui-debugger | AUTO | After frontend (if puppeteer) |
| debugger | MANUAL | User reports issue |
| test-engineer | AUTO | L2 testing phase |
| project-ops | AUTO | After milestones |

---

## Project Context

> {{PROJECT_DESCRIPTION}}

### Key Decisions

- [Document important decisions here]

### Notes

- [Add project notes, context, constraints]

---

## Session Resume

**When starting a new session, Claude should:**

1. Read the Workflow State below
2. Announce: "Resuming [project] - [current state]. Continue?"
3. On confirmation, continue from saved position
4. If state is unclear, ask for clarification

**Current Status:**
- Phase: L1 Planning (not started)
- Next: Begin with intent-guardian to capture promises

---

## Workflow State
```yaml
workflow:
  version: 1.0
  type: greenfield
  phase: L1                    # L1 (planning) or L2 (building)
  status: not_started          # not_started, in_progress, paused, complete
  mode: auto                   # auto (recommended) or manual
  initialized: {{DATE}}

l1:
  intent:
    status: pending            # pending, in_progress, complete
    output: null
  ux:
    status: pending
    output: null
  architecture:
    status: pending
    output: null
  planning:
    status: pending
    output: null

l2:
  current_feature: null
  current_step: null           # backend, frontend, testing, verification
  features: {}
  # Features will be populated after L1 planning completes

quality:
  last_review: null
  last_review_result: null     # pass, fail
  last_test_run: null
  last_test_result: null       # pass, fail
  open_issues: []

ci:
  last_check: null
  status: null                 # pass, fail, not_configured

session:
  last_updated: {{DATE}}
  last_action: "Project initialized"
  next_action: "User describes project → Begin L1 with intent-guardian"
  session_count: 0
```

---

## Workflow Commands

| Command | Description |
|---------|-------------|
| `/help` | Show all commands |
| `/status` | Show project progress |
| `/next` | Continue to next step |
| `/review [path]` | Code review specific files |
| `/verify` | Run verification checks |
| `/debug [issue]` | Start debug session |
| `/project commit` | Conventional commit |
| `/project release` | Release workflow |
| `/workflow status` | Show workflow state |
| `/workflow pause` | Pause auto-orchestration |
